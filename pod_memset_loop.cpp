// use -DUSE_MEMSET to compile memset version

#include <cassert>
#include <cstring>

struct A {
    int x;
    char y;
};

int main() {
    const int n = 1000000;
    A a[n];
    volatile int x = 0;
    for (int j = 0; j < 1000; ++j) {
        }
#endif
    }
}
